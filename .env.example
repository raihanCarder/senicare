# MongoDB Atlas (recommended)
# 1) Create an Atlas cluster + DB user
# 2) Allowlist your IP in Atlas Network Access
MONGODB_URI=mongodb+srv://longvnhl2005_db_user:VNHLlong2005@cluster0.i2zwsly.mongodb.net/guardian?retryWrites=true&w=majority&appName=Cluster0

# Auth (JWT)
# Generate a long random string. Do not commit the real value.
JWT_SECRET=change_me_to_a_long_random_secret
JWT_ALG=HS256
JWT_EXPIRES_MIN=60

# Set to true to require auth on protected routes (see backend/app/main.py).
REQUIRE_AUTH=false
